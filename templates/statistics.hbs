<section class="statistics">
  <span class="statistics__eyebrow">{{eyebrow}}</span>
  <h2 class="statistics__headline">{{title}}</h2>
  <div class="statistics__numbers">
    {{#each statisticsList}}
      {{{module this}}}
    {{/each}}
  </div>

</section>
